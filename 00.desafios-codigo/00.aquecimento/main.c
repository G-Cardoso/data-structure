// Gabriel Cardoso Mendes de Ataide
// 11811ECP008

#include <stdio.h> //Inclusão da biblioteca STDIO

int main(){
    int a, b;                  // Declarando variáveis A e B
    scanf("%d %d", &a, &b);    // Lendo (do teclado) e salvando os dados das variáveis A e B
    printf("X = %d\n", a + b); // Imprimindo o resultado da soma de A+B
    return 0;                  // Finalizando o programa e retornando 0.
}